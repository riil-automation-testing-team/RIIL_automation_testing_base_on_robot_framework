*** Settings ***
Resource  cheetah_element_keyword_resource.txt
Resource  riil_common_keyword_resource.txt

*** Keywords ***

登录猎豹
    [Arguments]  ${username}  ${password}
    打开浏览器
    隐式等待
    登录名称    ${username}
    登录密码    ${password}
    登录按钮

打开菜单项
    [Arguments]  ${parent_node}   ${child_node}
    展开菜单
    打开目录    ${parent_node}
    打开目录    ${child_node}
    等待 2s

校验IP基本信息
    输入空名称    IP分组    IP_name
    输入空地址    IP分组    IP_address
    输入多空格名称     IP分组    IP_name
    校验输入字符串  IP分组    IP_name     自动化测试IP组&quot;  不能输入特殊字符
    校验输入字符串  IP分组    IP_name     '&quot;%()/\:?<"";|&@#*   名称长度，不能超过20
    合法名称IP地址为空  IP分组    IP_name   Automation testing
    输入已存在IP分组名称  IP分组    IP_name   IP_address    Automation testing   172.17.161.1/24\n172.17.163.98\n172.17.161.1-172.17.161.200

校验IP基础信息标题
    [Arguments]  ${str}
    校验字符串   div     添加IP分组      ${str}

输入合法IP分组名称和地址
    [Arguments]  ${section}  ${xpath_name}  ${xpath_address}  ${name}  ${address}
    添加IP分组
    输入名称   ${section}  ${xpath_name}  ${name}
    输入名称   ${section}  ${xpath_address}  ${address}
    保存按钮
    等待 2s

输入已存在IP分组名称
    [Arguments]  ${section}  ${xpath_name}  ${xpath_address}   ${name}  ${address}
    输入合法IP分组名称和地址   ${section}  ${xpath_name}  ${xpath_address}   ${name}   ${address}
    添加IP分组
    输入名称   ${section}  ${xpath_name}  ${name}
    按键   ${section}  ${xpath_name}   TAB
    等待 2s
    校验字符串   div   已存在    已存在

合法名称IP地址为空
    [Arguments]     ${section}  ${xpath_name}  ${name}
    输入名称    ${section}  ${xpath_name}  ${name}
    保存按钮
    校验字符串   div   请输入IP地址     请输入IP地址

验证头部基础信息